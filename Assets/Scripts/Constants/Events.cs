namespace Constants
{
    public enum Events
    {
        OnBossDied,
        OnGameEnd,
        OnGameStart,
        OnGameSuspend,
        OnHintCollected,
        OnNotification,
        OnSignalerShutdown,
    }
}